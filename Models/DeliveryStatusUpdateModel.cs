using SecureTransparentDataExchange.Models.Enums;

namespace SecureTransparentDataExchange.Models
{
    public class DeliveryStatusUpdateModel
    {
        public ShipmentStatus Status { get; set; } // Uses the ShipmentStatus enum
    }
}
